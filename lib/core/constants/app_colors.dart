import 'package:flutter/material.dart';

class AppColors {
  // Premium “health + trust” palette
  static const Color primary = Color(0xFF1F7AE0);     // Trust Blue
  static const Color secondary = Color(0xFF13B887);   // Care Green
  static const Color background = Color(0xFFF7FAFF);  // Soft clean background
  static const Color surface = Colors.white;
  static const Color text = Color(0xFF0F172A);        // Slate
  static const Color muted = Color(0xFF64748B);       // Muted slate
}
